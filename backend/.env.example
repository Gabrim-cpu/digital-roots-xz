PORT=5000

# PostgreSQL (matches docker-compose.yml — run setup.bat after starting Docker Desktop)
DB_HOST=localhost
DB_PORT=5433
DB_USER=xz_user
DB_PASSWORD=xz_password_123
DB_NAME=xz_database

# If you see "password authentication failed for user xz_user":
#   cd backend && npm run db:reset

# Firebase Admin (path relative to backend/)
FIREBASE_PROJECT_ID=C:\Users\Upclass Telco Sarl\digital-roots-xz\backend\.env.example
